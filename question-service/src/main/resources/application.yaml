server:
  port: 9020

spring:
  application:
    name: QUESTIONSERVICE

  datasource:
    url: jdbc:postgresql://localhost:5432/quiz
    username: postgres
    password: postgres

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka